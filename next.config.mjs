/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['static.wtable.co.kr', 'lh3.googleusercontent.com', 'images.pexels.com'],
    },
};

export default nextConfig;